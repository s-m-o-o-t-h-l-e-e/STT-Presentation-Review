# reports

분석 결과를 PDF 리포트로 만드는 폴더입니다.

## 파일

- `pdf_report.py`
  - 종합 점수, WPM, 추임새, 어휘 개선, 자료 일치율, 예상 질문을 PDF 형식으로 정리합니다.
  - 화면에서 확인한 분석 결과를 제출용 리포트 형태로 묶습니다.

## 출력

PDF 파일은 실행 결과에 따라 `output/` 또는 다운로드 응답으로 생성됩니다. `output/`은 생성물 폴더이므로 Git에는 올리지 않습니다.
